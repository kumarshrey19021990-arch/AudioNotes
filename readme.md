# Audiopen replica (greenfield)

Project lives in `audiopen/`.

- Dev: `cd audiopen && npm install && npm run db:migrate && npm run dev`
- Optional: set `OPENAI_API_KEY` for real transcription + note generation (otherwise mock output is used)